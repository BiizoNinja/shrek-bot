const artificialIntelligence = require('./ai.js');

class Alexa {

  constructor() {
  }
  
  async getReply(message) {
    if(message === '' || null) {
      throw new TypeError('Message Cannot be empty in getReply()')
    }
    return await artificialIntelligence(message)
  }

}

module.exports = Alexa;